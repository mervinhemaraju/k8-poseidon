# Uptime Kuma configuration
image:
  repository: louislam/uptime-kuma
  tag: "2.0.0-beta.4"
  pullPolicy: IfNotPresent

# Service configuration
service:
  type: ClusterIP
  port: 3001
  targetPort: 3001

# Resources
resources:
  requests:
    cpu: "100m"
    memory: "128Mi"
  limits:
    cpu: "500m"
    memory: "512Mi"

# Storage Persistency - disabled since we're using external MariaDB
persistence:
  enabled: false

volume:
  enabled: false

mariadb:
  enabled: false

# Environment variables for MariaDB connection
podEnv:
  - name: UPTIME_KUMA_DB_TYPE
    value: "mariadb"
  - name: UPTIME_KUMA_DB_HOSTNAME
    value: "10.18.20.10"
  - name: UPTIME_KUMA_DB_PORT
    value: "3306"
  - name: UPTIME_KUMA_DB_NAME
    value: "uptimekuma"
  - name: UPTIME_KUMA_DB_USERNAME
    value: "th3pl4gu3"
  - name: UPTIME_KUMA_DB_PASSWORD
    valueFrom:
      secretKeyRef:
        name: uptime-kuma-db-secret
        key: password

# Node selector (optional - for ARM nodes if needed)
nodeSelector: {}

# Tolerations
tolerations: []

# Affinity
affinity: {}

# Replica count
replicaCount: 1

# Update strategy
strategy:
  type: RollingUpdate
  rollingUpdate:
    maxUnavailable: 0
    maxSurge: 1